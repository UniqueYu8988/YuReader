# YuReader 长期协作说明

## 项目身份

YuReader 是一个本地优先的 AI 辅助阅读器，面向已经扫描或整理为 Markdown 的实用书籍。

它的核心体验不是在网页里内置一个聊天机器人，而是把长书稳定地拆成适合阅读和侧边栏 AI 获取的章节，让用户可以：

1. 从书架快速进入准确的章节位置；
2. 让浏览器侧边栏 AI 直接读取当前页面并分析；
3. 把有价值的总结和个人理解回填到对应章节；
4. 将原书、清洗结果、阅读位置和笔记长期保存在本地；
5. 将来从一份电子书出发，逐步打通导入、清洗、切分、阅读和导出。

YuReader 来源于 YuQuiz 已验证的阅读体验和视觉系统，但它是独立项目。不要修改或依赖 `C:\Users\Yu\Documents\YuQuiz` 的运行数据，也不要把答题、模拟考试、错题统计等业务重新搬进来。

## 当前真实状态

当前版本是 `0.1.0`，已经实现：

- 扫描 `content/` 中的 Markdown 文件；
- 按书籍和二至六级标题建立书架与章节；
- 搜索书籍和章节；
- 阅读当前章节并在同书章节间切换；
- 展示基础 Markdown 标题、列表和表格；
- 为每个章节自动保存本地笔记；
- 深浅色主题、侧边栏、阅读工具条和移动端布局；
- 无账号、无云端上传、无模型 API 依赖。

当前尚未实现：

- PDF、EPUB 或图片书籍的导入；
- MinerU/OCR 调度；
- OCR 文本清洗、层级修复和质量报告；
- 可靠的语义切片与原文位置映射；
- 阅读进度和最近位置；
- Obsidian 同步；
- 跨书检索或本地语义索引。

不要在文档或界面中把尚未实现的能力写成现有功能。

## 开始工作前

每个新任务先依次阅读：

1. `AGENTS.md`
2. `README.md`
3. `docs/STATUS.md`
4. 与当前任务直接相关的代码和真实样本

先让现有系统和样本说明问题，再决定实现。不要仅凭愿景一次搭建庞大的处理平台。

完成有意义的阶段后更新 `docs/STATUS.md`，记录完成了什么、如何验证、仍有什么已知问题、下一步是什么。不要把流水账写进状态文档。

## 产品原则

### 1. 从真实书籍出发

优先用一本真实的扫描后 Markdown 作为样本，完成小范围闭环，再扩展到更多格式和批量任务。第一阶段优先支持 Markdown 导入，不要立即把 PDF、MinerU、OCR、外部模型和复杂任务队列同时接入。

### 2. 原始内容永不覆盖

任何导入和清洗都必须保留原始输入。自动修复、人工校订、阅读切片和导出物应当是可追溯的派生结果。

处理失败不能损坏原书，也不能把半成品当作正式内容发布到书架。

### 3. 阅读内容不是摘要

YuReader 可以修复层级、标点、断行、OCR 噪声和章节边界，但不能为了简洁擅自删掉原书的重要信息，也不能把 AI 总结冒充原文。

界面中必须能够区分：

- 原始内容；
- 清洗后的正文；
- 为侧边栏 AI 准备的阅读片段；
- AI 或用户写入的笔记。

### 4. AI 是可替换的助手

默认工作流依赖浏览器侧边栏 AI 读取当前页面，不假设用户拥有付费 API。需要模型参与的离线清洗必须设计为可选步骤，并保留不调用模型也能运行的确定性流程。

模型输出不能未经校验直接覆盖正文。结构、数量、标题连续性、空内容、异常缩短和来源映射应尽量由程序检查。

### 5. 本地优先

书籍、笔记和处理产物默认只保存在本机。不得静默上传文本、密钥或个人笔记。若以后加入联网能力，必须明确展示用途并允许关闭。

### 6. 正文从第一章开始

正式阅读内容必须从原书目录中第一个可靠的“第一章”标题开始。书目信息、版权页、前言、目录和其他第一章以前的前置单元只保留在原始归档与来源证据中，不进入 YuReader 书架的正文页面。目录只用于确认章节顺序和边界，不作为需要展示的正文；若无法可靠识别第一章，导入应阻断而不是猜测。

## 推荐内容管线

长期目标采用以下可分阶段验证的管线：

```text
电子书或扫描件
  -> 原始输入归档
  -> OCR / MinerU 原始 Markdown
  -> 确定性清洗
  -> 层级与章节修复
  -> 可选 AI 辅助校订
  -> 质量检查与人工复核队列
  -> 语义切片及来源映射
  -> 发布到 YuReader 书架
  -> 侧边栏 AI 阅读与笔记回填
  -> Obsidian / Markdown 导出
```

MinerU 应被视为外部工作器，通过适配层调用；不要复制或合并 MinerU 项目源码。YuReader 负责管理输入、状态、产物和阅读体验。

建议未来每本书使用类似结构，但在实现前允许根据真实样本调整：

```text
workspace/<book-id>/
  manifest.json
  original/
  raw/
  cleaned/
  slices/
  assets/
  reports/
```

`manifest.json` 应保存稳定书籍 ID、书名、来源文件、处理阶段、内容版本和产物路径。章节与笔记不要只依赖标题文字绑定，应使用稳定 ID 和来源映射。

## 下一阶段优先级

按以下顺序推进，除非用户明确改变目标：

1. 用一份真实 Markdown 书籍打通最小导入闭环；
2. 生成稳定的书籍清单、章节树和来源映射；
3. 建立确定性清洗与质量报告，不调用模型也可重复运行；
4. 设计适合侧边栏 AI 单次读取范围的语义切片；
5. 在 YuReader 中切换原文、清洗正文和阅读片段；
6. 保存阅读进度、最近打开位置和章节笔记；
7. 接入 Obsidian 目录映射与 Markdown 导出；
8. 最后再封装 PDF/MinerU 导入和批量任务界面。

第一里程碑不是“支持所有电子书”，而是“一本真实书能够无损导入、正确分章、稳定阅读、可回填笔记，并且所有结果可追溯”。

## 视觉与交互

YuReader 继承 YuQuiz 已形成的安静、精致、偏 Anthropic 气质的设计语言：暖白纸张、克制的橙色强调、衬线阅读标题、无衬线操作界面、细线分隔、充足留白和短促自然的动效。

必须遵守：

- `static/styles.css` 是来自 YuQuiz 的视觉基线；不要随意重写。
- 阅读器特有样式放在 `static/reader.css`。
- 优先复用现有组件、间距、颜色和动画变量。
- 使用 Lucide 图标；熟悉操作优先使用图标和 tooltip，避免堆叠文字按钮。
- 不做营销首页，打开后直接进入可用的书架与阅读体验。
- 不使用大面积渐变、装饰光球、嵌套卡片和过多圆角矩形。
- 阅读正文优先保证层级、行宽、行距、表格和移动端可读性。
- 动画服务于空间关系和状态变化，不做廉价弹跳或无意义闪烁。
- 深浅主题、桌面和平板布局都必须验证。

如果要改变整体设计语言，必须先解释为什么现有 YuQuiz 基线不能满足需求，并得到用户确认。

## 代码与目录

当前主要文件：

- `app.py`：本地 HTTP 服务、书架扫描、章节接口和笔记保存；
- `static/index.html`：阅读器页面骨架；
- `static/app.js`：书架、章节、Markdown 渲染和笔记交互；
- `static/styles.css`：YuQuiz 视觉基线；
- `static/reader.css`：YuReader 专用样式；
- `content/`：当前发布到书架的 Markdown，默认不进版本库；
- `data/notes/`：章节笔记，默认不进版本库；
- `docs/STATUS.md`：最近一次可靠状态和后续事项。

代码修改应小步、可验证。结构化内容优先使用解析器和数据结构，不要依赖脆弱的全局字符串替换。新增抽象必须解决真实重复或边界问题。

不要清理或移动用户的书籍、OCR 产物和笔记，除非用户明确要求。样本处理默认写入项目工作区中的新目录，不改写 OneDrive 原文件。

## 运行与验证

启动：

```powershell
cd C:\Users\Yu\Documents\YuReader
python app.py
```

默认地址：`http://127.0.0.1:8775`

也可以双击 `启动 YuReader.bat`。
桌面启动可使用 `YuReader.lnk`；它指向项目内的 `YuReader.exe` 原生启动器，再复用现有 `启动 YuReader.bat`。`tools/create_desktop_shortcut.ps1` 会同时生成桌面与开始菜单“所有应用”入口，若快捷方式不存在可重新运行。

每次涉及代码的改动至少执行与风险相匹配的验证：

- `python -m py_compile app.py`
- `node --check static/app.js`
- 请求 `/api/health` 与 `/api/bootstrap`
- 使用真实书籍验证书架、章节顺序、正文、表格和笔记自动保存
- 使用浏览器检查控制台错误
- 桌面与移动端截图检查布局、文字溢出、遮挡和空白页面

不要为了测试关闭用户正在使用的其他项目。需要重启 YuReader 时先确认端口和进程，只操作 YuReader 自己的服务。

## 完成标准

一个功能只有在以下条件满足后才算完成：

- 行为与真实样本一致，不只通过人工构造数据；
- 失败不会破坏原始内容或现有笔记；
- 页面没有明显控制台错误、布局溢出或移动端退化；
- 新的数据结构和接口有简短文档；
- `docs/STATUS.md` 已记录重要状态；
- 用户可以理解产物在哪里、如何继续使用。

## 明确避免

- 不要修改 YuQuiz 来完成 YuReader 功能。
- 不要把 YuReader 再做成题库应用。
- 不要从零设计另一套 UI。
- 不要默认绑定某一家付费模型或 API。
- 不要在没有来源映射时大规模重写书籍。
- 不要把自动清洗结果直接覆盖原文。
- 不要一次实现完整宏大架构而没有一本真实书的闭环。
- 不要把实验文件、真实书籍、笔记、密钥和大型处理产物提交到公开仓库。
